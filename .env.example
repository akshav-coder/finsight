VITE_ENABLE_AI=false
VITE_ENABLE_PAYMENTS=false

VITE_GEMINI_API_KEY=

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=

VITE_RAZORPAY_KEY_ID=

# Server-side only (Vercel), never prefix with VITE_
GEMINI_API_KEY=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
FIREBASE_PROJECT_ID=
# Full JSON content of a Firebase service account key (Project Settings ->
# Service Accounts -> Generate new private key), as a single-line string.
FIREBASE_SERVICE_ACCOUNT_KEY=
# Restrict CORS on /api/* to your real domain in production, e.g.
# https://finsight.vercel.app — defaults to "*" (open) if unset.
ALLOWED_ORIGIN=
